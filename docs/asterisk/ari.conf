; ARI user for the Node.js backend.
; read_only=no allows the app to originate, bridge, hangup.

[general]
enabled = yes
pretty = yes

[myariuser]
type = user
read_only = no
password = change-me
password_format = plain
